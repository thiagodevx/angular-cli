import { SearchBox2Directive } from './search-box2.directive';

describe('SearchBox2Directive', () => {
  it('should create an instance', () => {
    const directive = new SearchBox2Directive();
    expect(directive).toBeTruthy();
  });
});
